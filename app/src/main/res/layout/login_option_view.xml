<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent">


    <ImageButton
        android:id="@+id/professional_login_image_view"
        android:layout_width="match_parent"
        android:layout_height="256dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="selectLogin"
        app:srcCompat="@drawable/professional_login_image"
        android:tag="0"/>

    <ImageButton
        android:id="@+id/user_login_image_view"
        android:layout_width="match_parent"
        android:layout_height="285dp"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/professional_login_image_view"
        android:onClick="selectLogin"
        app:srcCompat="@drawable/user_login_image"
        android:tag="1"/>

</RelativeLayout>